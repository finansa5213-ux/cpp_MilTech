# Усі текстові файли зберігаються у сховищі з кінцями рядків LF незалежно
# від того, з якої системи зроблено коміт. Інакше коміт із Windows перевів би
# увесь код на CRLF і перший же diff показав би зміненими всі рядки.
* text=auto eol=lf

# Двійкові файли не чіпати
*.png binary
*.jpg binary
*.pdf binary
*.zip binary
*.ulg binary
